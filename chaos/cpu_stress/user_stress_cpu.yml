kind: StressChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  namespace: ts
  name: user-stress-cpu
spec:
  selector:
    namespaces:
      - ts
    labelSelectors:
      app: ts-user-service
  mode: one
  stressors:
    cpu:
      workers: 4
      load: 100
  duration: 5m